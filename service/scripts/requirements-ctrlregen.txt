# Dependencies for the optional CtrlRegen pixel-removal backend
# (mertizci/noai-watermark). The backend is cloned at a pinned commit by
# setup_ctrlregen.sh and imported at runtime; it is never bundled.
#
# torch is installed separately in setup_ctrlregen.sh with the correct
# platform index (CUDA or CPU), so it is intentionally not listed here.
#
# The pinned upstream checkout (NOAI_WATERMARK_REF, currently the tip of
# mertizci/noai-watermark main) is the same code that ships as the maintained
# `noai-watermark` PyPI package (v0.1.29), which declares
# diffusers>=0.25.0 / transformers>=4.35.0. These pins are therefore the
# current versions, not research-era holds. Keep them current and re-validate
# the pipeline on a device before changing them.
#
# This file is only ever installed inside the dedicated venv created by
# setup_ctrlregen.sh (service profile "ctrlregen"), never into the main
# service image. Do not merge these pins into the core requirements or the
# Docker images.
diffusers==0.39.0
transformers==5.15.1
accelerate==1.14.0
controlnet-aux==0.0.10
color-matcher==0.6.0
safetensors==0.8.0
huggingface_hub==1.28.0
Pillow==12.3.0
piexif==1.1.3